<!doctype html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="theme-color" content="#080808">

  <title>
    {%- if page_title == shop.name -%}
      {{ shop.name }} — Premium Streetwear
    {%- else -%}
      {{ page_title }} — {{ shop.name }}
    {%- endif -%}
  </title>

  {%- if page_description -%}
    <meta name="description" content="{{ page_description | escape }}">
  {%- endif -%}

  <!-- Open Graph -->
  <meta property="og:site_name" content="{{ shop.name }}">
  <meta property="og:title" content="{{ page_title | escape }}">
  <meta property="og:type" content="website">
  <meta property="og:url" content="{{ request.origin }}{{ request.path }}">
  {%- if page_image -%}
    <meta property="og:image" content="{{ page_image | image_url: width: 1200 }}">
  {%- endif -%}

  {{ content_for_header }}

  <link rel="stylesheet" href="{{ 'theme.css' | asset_url }}">
  <link rel="icon" href="{{ 'favicon.svg' | asset_url }}" type="image/svg+xml">
</head>

<body class="site-body">
  <!-- Gold top stripe -->
  <div class="gold-stripe" aria-hidden="true"></div>

  <!-- Skip to content -->
  <a class="skip-link" href="#MainContent">Skip to content</a>

  {%- section 'header' -%}

  <!-- Search overlay -->
  <div id="search-overlay" class="search-overlay" role="dialog" aria-modal="true" aria-label="Search" hidden>
    <button class="search-close" id="search-close" aria-label="Close search">
      <svg width="24" height="24" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
      </svg>
    </button>
    <div class="search-inner">
      <p class="search-eyebrow">Search</p>
      <form action="/search" method="get" class="search-form" role="search">
        <div class="search-input-wrap">
          <svg class="search-icon" width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
          </svg>
          <input type="search" name="q" id="search-input" placeholder="Search products..." aria-label="Search products" autocomplete="off" class="search-input">
        </div>
      </form>
      <div id="search-results" class="search-results"></div>
      <div id="search-suggestions" class="search-suggestions">
        <p class="search-suggestions-label">Popular categories</p>
        <div class="search-pills">
          <a href="/collections/new-drops" class="search-pill">New Drops</a>
          <a href="/collections/tops" class="search-pill">Tops</a>
          <a href="/collections/bottoms" class="search-pill">Bottoms</a>
          <a href="/collections/sets" class="search-pill">Sets</a>
          <a href="/collections/jackets" class="search-pill">Jackets</a>
          <a href="/collections/accessories" class="search-pill">Accessories</a>
        </div>
      </div>
    </div>
  </div>

  <!-- Cart drawer -->
  <div id="cart-drawer" class="cart-drawer" role="dialog" aria-modal="true" aria-label="Shopping cart" hidden>
    <div class="cart-drawer-overlay" id="cart-overlay"></div>
    <div class="cart-drawer-panel">
      <div class="cart-drawer-header">
        <h2 class="cart-drawer-title">Your Cart <span id="cart-count-badge" class="cart-badge"></span></h2>
        <button class="cart-drawer-close" id="cart-close" aria-label="Close cart">
          <svg width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
          </svg>
        </button>
      </div>
      <div id="cart-drawer-items" class="cart-drawer-items"></div>
      <div id="cart-drawer-footer" class="cart-drawer-footer" hidden>
        <div class="cart-drawer-totals">
          <span>Subtotal</span>
          <span id="cart-subtotal"></span>
        </div>
        <p class="cart-shipping-note">Shipping calculated at checkout</p>
        <a href="/checkout" class="btn-gold cart-checkout-btn">Proceed to Checkout</a>
        <a href="/cart" class="cart-view-link">View full cart</a>
      </div>
      <div id="cart-empty" class="cart-empty">
        <p>Your cart is empty.</p>
        <a href="/collections/all" class="btn-gold" style="display:inline-block;margin-top:1rem">Shop Now</a>
      </div>
    </div>
  </div>

  <main id="MainContent" role="main" tabindex="-1">
    {{ content_for_layout }}
  </main>

  {%- section 'footer' -%}

  <!-- Cookie banner -->
  <div id="cookie-banner" class="cookie-banner" hidden>
    <div class="cookie-banner-inner">
      <p class="cookie-text">
        We use essential cookies to make our site work. By clicking "Accept All" you agree to our
        <a href="/pages/cookie-policy" class="cookie-link">Cookie Policy</a>.
      </p>
      <div class="cookie-actions">
        <button id="cookie-accept" class="btn-gold cookie-btn">Accept All</button>
        <button id="cookie-decline" class="cookie-decline">Decline</button>
      </div>
    </div>
  </div>

  <!-- Sticky mobile shop button -->
  <a href="/collections/all" id="sticky-shop-btn" class="sticky-shop-btn" aria-label="Shop Now" hidden>
    SHOP NOW
  </a>

  <script src="{{ 'theme.js' | asset_url }}" defer></script>

  <!-- Email popup -->
  <div id="email-popup" class="email-popup" hidden>
    <div class="email-popup-overlay" id="email-popup-overlay"></div>
    <div class="email-popup-panel">
      <button class="email-popup-close" id="email-popup-close" aria-label="Close popup">
        <svg width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
        </svg>
      </button>
      <div class="email-popup-eyebrow-row">
        <div class="email-popup-rule"></div>
        <span class="email-popup-eyebrow">Exclusive Access</span>
        <div class="email-popup-rule"></div>
      </div>
      <h2 class="email-popup-title">JOIN THE INNER CIRCLE</h2>
      <p class="email-popup-subtitle">Get 10% off your first order + early access to every drop. No spam, ever.</p>
      <form id="email-popup-form" class="email-popup-form">
        <input type="email" name="email" class="email-popup-input" placeholder="Enter your email" required aria-label="Email address">
        <button type="submit" class="btn-gold email-popup-submit">CLAIM MY 10% OFF</button>
      </form>
      <button class="email-popup-dismiss" id="email-popup-dismiss">NO THANKS, I'LL PAY FULL PRICE</button>
    </div>
  </div>

  <script>
  (function(){
    var KEY = 'ic_popup_v1';
    var popup = document.getElementById('email-popup');
    if (!popup) return;
    if (localStorage.getItem(KEY)) return;
    setTimeout(function(){ popup.removeAttribute('hidden'); }, 2500);
    function close() {
      popup.setAttribute('hidden', '');
      localStorage.setItem(KEY, '1');
    }
    document.getElementById('email-popup-close').addEventListener('click', close);
    document.getElementById('email-popup-dismiss').addEventListener('click', close);
    document.getElementById('email-popup-overlay').addEventListener('click', close);
    document.getElementById('email-popup-form').addEventListener('submit', function(e){
      e.preventDefault();
      var form = this;
      form.innerHTML = '<p class="email-popup-success">You\'re in. Check your inbox for your 10% off code.</p>';
      setTimeout(close, 3000);
    });
  })();
  </script>
</body>
</html>
